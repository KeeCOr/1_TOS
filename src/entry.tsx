import { createRoot } from 'react-dom/client';
import SwordmastersAscent from './components/SwordmastersAscent';

const el = document.getElementById('root');
if (el) createRoot(el).render(<SwordmastersAscent />);
